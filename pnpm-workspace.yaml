packages:
  - 'apps/*'
  - 'packages/*'
# Postinstall scripts are denied by default; this one is approved explicitly.
# esbuild ships a prebuilt binary that vitest, drizzle-kit and Next all need.
allowBuilds:
  esbuild: true
