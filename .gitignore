node_modules/
.next/
dist/
.turbo/
*.tsbuildinfo
# Every env file is a secret except the checked-in template. The explicit
# pattern matters: `.env.neon` holds the production connection strings and was
# not covered by the three rules that used to be here, so `git add -A` would
# have committed it.
.env
.env.*
!.env.example
.DS_Store
coverage/
apps/web/.env.local
scripts/.mutation-apply.mts

# Written by `next dev`; disabled via agentRules in next.config.ts.
apps/web/AGENTS.md
apps/web/CLAUDE.md

# cron job logs
.cron-*.log
data/private/
